return {
  "nvim-telescope/telescope.nvim",
  tag = "0.1.8",
  opts = {
    defaults = {
      layout_config = {
        prompt_position = "top", -- put search box at top
      },
      sorting_strategy = "ascending", -- results go down from the prompt
    },
  },
}
